/// @description Insert description here

one = instance_create_layer(x, y, layer, objBaseTop);
eye = instance_create_layer(x + 48, y + 62, layer, objBaseEyeH);
two = instance_create_layer(x, y + 80, layer, objBaseBottom);

shipInRange = false;
