/// @description Insert description here

top = instance_create_layer(x, y, layer, objBaseTop);
eye = instance_create_layer(x + 48, y + 62, layer, objBaseEye);
bottom = instance_create_layer(x, y + 80, layer, objBaseBottom);
