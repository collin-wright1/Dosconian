/// @description Insert description here

one = instance_create_layer(x, y, layer, objBaseLeft);
eye = instance_create_layer(x + 62, y + 48, layer, objBaseEyeV);
two = instance_create_layer(x + 80, y, layer, objBaseRight);

shipInRange = false;
